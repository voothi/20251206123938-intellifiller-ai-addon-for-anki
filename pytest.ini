[pytest]
testpaths = tests
norecursedirs = IntelliFiller/vendor vendor .git .kilocode .qoder .agent
markers =
    security: tests that verify credential obfuscation and storage guarantees
    worker: tests for the background thread worker
    backup: tests for the backup manager
    config: tests for the configuration manager
addopts = -ra
